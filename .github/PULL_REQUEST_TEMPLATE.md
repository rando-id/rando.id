<!--
Thanks for the PR! A few notes:

- Title format: `feat(scope):` / `fix(scope):` / `chore(scope):` / `docs(scope):`.
- Link the ticket: `Closes #N` or `Refs #N`. Skip only for typo fixes.
- Tests + typecheck must pass locally before the PR is ready for review.
-->

## Summary

<!-- 1–3 bullets on what changed and why. Focus on the why. -->

-
-

Closes #

## Test plan

<!-- How you verified this. Be concrete — commands, scenarios, expected outputs. -->

- [ ] `pnpm typecheck`
- [ ] `pnpm test`
- [ ] If a Vercel preview is needed to validate this change, I've added the **`deploy-preview`** label (previews are opt-in for all authors; see CONTRIBUTING.md → "Preview deploys are opt-in")
- [ ]

## Screenshots / output

<!-- For UI changes: before / after screenshots. For CLI / infra: relevant terminal output. Delete if N/A. -->

## Notes for reviewers

<!-- Anything non-obvious: trade-offs taken, things deliberately deferred, areas you'd like extra eyes on. Delete if N/A. -->
